/*
package com.hackerrank.github.utilities;

import java.util.Comparator;

public class ActorComparator implements Comparator<Actor2> {

    @Override
    public int compare(Actor2 o1, Actor2 o2) {
        if(o1.getCount()>o2.getCount()){
            return 1;
        }else if(o1.getCount()>o2.getCount()){
            return -1;
        }else{
            if(o1.getLatestDate().after(o2.getLatestDate())){
                return 1;
            }else if(o1.getLatestDate().before(o2.getLatestDate())) {
                return -1;
            }else{
                return o1.getLogin().compareTo(o2.getLogin());
            }
        }
    }
}
*/
